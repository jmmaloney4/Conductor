matrix:
  include:
    - os: linux
      dist: trusty
      sudo: required
      language: cpp
    - os: osx
      osx_image: xcode8.3
      language: objective-c
      sudo: required

install:
  - eval "$(curl -sL https://swiftenv.fuller.li/install.sh)"

script:
  - swift build

notifications:
  email:
    on_success: never
    on_failure: change
